function tellStory() {
    // nounArray = querySelector('#noun')
    // lowercase String split()
    // repeat for adjectives & verbs

    const myStory = `Once upon a time there were four ${nounArray[0]}s named Geroge, Bill, Harry, and Ron...`

    // get a reference to the querySelector('#story') = myStory
}